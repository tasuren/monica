{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "draw",
    "description": "Capability for the draw window",
    "windows": ["draw-*"],
    "permissions": [
        "core:default",
        "core:event:allow-listen",
        "core:event:allow-unlisten"
    ]
}
